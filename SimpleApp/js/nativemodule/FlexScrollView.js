'use strict';


import {requireNativeComponent} from 'react-native';


/**
 * 第一个参数为原生视图的名称，第二个参数是一个描述组件接口的对象
 */
var FlexScrollView = requireNativeComponent('FlexScrollView');

module.exports = FlexScrollView;